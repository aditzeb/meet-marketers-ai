/// App Configuration Template for Meet Marketers AI
/// Copy this file to `lib/core/config/app_config.dart` and insert your API keys.
/// `lib/core/config/app_config.dart` is ignored by git to protect secrets.
class AppConfig {
  static const String geminiApiKey = 'YOUR_GEMINI_API_KEY';
}
